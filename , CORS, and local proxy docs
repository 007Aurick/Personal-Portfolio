[1mdiff --git a/.env.example b/.env.example[m
[1mindex 9b6e4867..eacf1664 100644[m
[1m--- a/.env.example[m
[1m+++ b/.env.example[m
[36m@@ -18,17 +18,26 @@[m [mREACT_APP_SPOTIFY_CLIENT_ID=[m
 # -----------------------------------------------------------------------------[m
 # Public feed (deployed site shows YOUR Spotify to all visitors, no “Connect”)[m
 # -----------------------------------------------------------------------------[m
[31m-# Set to true on Vercel (Environment Variables for Production). Leave false locally[m
[31m-# if you still want to test PKCE with “Connect Spotify”.[m
[32m+[m[32m# Set to true on Vercel (Environment Variables for Production).[m
 # REACT_APP_SPOTIFY_PUBLIC_FEED=true[m
 #[m
[31m-# Only needed if the API is not on the same origin (leave unset for Vercel):[m
[31m-# REACT_APP_SPOTIFY_API_BASE=https://www.yoursite.com[m
[32m+[m[32m# Same-origin API (Vercel): leave REACT_APP_SPOTIFY_API_BASE unset — /api/spotify is on your domain.[m
[32m+[m[32m#[m
[32m+[m[32m# --- Local dev (npm start, recommended) ---[m
[32m+[m[32m# package.json has "proxy" → your production origin. The browser calls /api/spotify on :3000; CRA forwards it.[m
[32m+[m[32m# REACT_APP_SPOTIFY_PUBLIC_FEED=true[m
[32m+[m[32m# Do NOT set REACT_APP_SPOTIFY_API_BASE (empty = same origin = proxy, no CORS issues).[m
[32m+[m[32m#[m
[32m+[m[32m# --- Local dev (alternative): call production URL directly ---[m
[32m+[m[32m# Requires the deployed api/spotify.js CORS allowlist (or you will see "Failed to fetch").[m
[32m+[m[32m# REACT_APP_SPOTIFY_PUBLIC_FEED=true[m
[32m+[m[32m# REACT_APP_SPOTIFY_API_BASE=https://www.aurickanwar.com[m
 #[m
 # --- Vercel server env (Settings → Environment Variables) — NOT prefixed REACT_ ---[m
[31m-# SPOTIFY_CLIENT_ID=           same value as REACT_APP_SPOTIFY_CLIENT_ID[m
[31m-# SPOTIFY_REFRESH_TOKEN=       long-lived refresh token (see below)[m
[32m+[m[32m# SPOTIFY_CLIENT_ID=           same as Spotify Client ID (or rely on REACT_APP_SPOTIFY_CLIENT_ID below — api/spotify accepts either)[m
[32m+[m[32m# SPOTIFY_REFRESH_TOKEN=       long-lived refresh token (required — see below)[m
 # SPOTIFY_CLIENT_SECRET=      optional; only if your app type requires it for refresh[m
[32m+[m[32m# SPOTIFY_CORS_ORIGINS=       optional; e.g. http://127.0.0.1:5173 if you use another dev port (comma-separated)[m
 #[m
 # How to get SPOTIFY_REFRESH_TOKEN (one time):[m
 # 1. Locally: set REACT_APP_SPOTIFY_CLIENT_ID, run npm start, click Connect Spotify, finish OAuth.[m
[1mdiff --git a/api/spotify.js b/api/spotify.js[m
[1mindex d01727fb..408ca069 100644[m
[1m--- a/api/spotify.js[m
[1m+++ b/api/spotify.js[m
[36m@@ -2,26 +2,58 @@[m
  * Vercel Serverless — serves your Spotify data to all visitors using a refresh token[m
  * (never exposed to the browser). Set env vars in the Vercel project dashboard.[m
  *[m
[31m- * Required: SPOTIFY_CLIENT_ID, SPOTIFY_REFRESH_TOKEN[m
[32m+[m[32m * Required: SPOTIFY_REFRESH_TOKEN, and either SPOTIFY_CLIENT_ID or REACT_APP_SPOTIFY_CLIENT_ID[m
  * Optional: SPOTIFY_CLIENT_SECRET (only if your Spotify app type requires it for refresh)[m
[32m+[m[32m * Optional: SPOTIFY_CORS_ORIGINS — comma-separated extra browser origins (default allows CRA on :3000)[m
  */[m
 [m
 const TOKEN_URL = 'https://accounts.spotify.com/api/token';[m
 const API = 'https://api.spotify.com/v1';[m
 [m
[32m+[m[32m/** Local CRA dev calls production `/api/spotify` cross-origin — browsers require CORS. */[m
[32m+[m[32mfunction applyCors(req, res) {[m
[32m+[m[32m  const origin = req.headers.origin;[m
[32m+[m[32m  const extras = (process.env.SPOTIFY_CORS_ORIGINS || '')[m
[32m+[m[32m    .split(',')[m
[32m+[m[32m    .map((s) => s.trim())[m
[32m+[m[32m    .filter(Boolean);[m
[32m+[m[32m  const allowed = new Set([[m
[32m+[m[32m    'http://127.0.0.1:3000',[m
[32m+[m[32m    'http://localhost:3000',[m
[32m+[m[32m    'http://127.0.0.1:3001',[m
[32m+[m[32m    'http://localhost:3001',[m
[32m+[m[32m    ...extras,[m
[32m+[m[32m  ]);[m
[32m+[m[32m  if (origin && allowed.has(origin)) {[m
[32m+[m[32m    res.setHeader('Access-Control-Allow-Origin', origin);[m
[32m+[m[32m    res.setHeader('Vary', 'Origin');[m
[32m+[m[32m  }[m
[32m+[m[32m}[m
[32m+[m
 let cached = { access: null, exp: 0 };[m
 [m
[32m+[m[32mfunction envTrim(name) {[m
[32m+[m[32m  return String(process.env[name] || '').trim();[m
[32m+[m[32m}[m
[32m+[m
 async function getAccessToken() {[m
   const now = Date.now();[m
   if (cached.access && now < cached.exp - 60_000) return cached.access;[m
 [m
[31m-  const clientId = process.env.SPOTIFY_CLIENT_ID;[m
[31m-  const refreshToken = process.env.SPOTIFY_REFRESH_TOKEN;[m
[31m-  const clientSecret = process.env.SPOTIFY_CLIENT_SECRET;[m
[32m+[m[32m  /** Many projects only set REACT_APP_SPOTIFY_CLIENT_ID on Vercel — reuse for token refresh. */[m
[32m+[m[32m  const clientId = envTrim('SPOTIFY_CLIENT_ID') || envTrim('REACT_APP_SPOTIFY_CLIENT_ID');[m
[32m+[m[32m  const refreshToken = envTrim('SPOTIFY_REFRESH_TOKEN');[m
[32m+[m[32m  const clientSecret = envTrim('SPOTIFY_CLIENT_SECRET');[m
 [m
   if (!clientId || !refreshToken) {[m
[31m-    const err = new Error('Server misconfigured: missing SPOTIFY_CLIENT_ID or SPOTIFY_REFRESH_TOKEN');[m
[32m+[m[32m    const missing = [];[m
[32m+[m[32m    if (!clientId) missing.push('SPOTIFY_CLIENT_ID (or set REACT_APP_SPOTIFY_CLIENT_ID to the same Client ID)');[m
[32m+[m[32m    if (!refreshToken) missing.push('SPOTIFY_REFRESH_TOKEN (OAuth refresh token from Session Storage — not the Client ID)');[m
[32m+[m[32m    const err = new Error(`Server misconfigured: missing ${missing.join('; ')}`);[m
     err.status = 503;[m
[32m+[m[32m    err.missing = missing;[m
[32m+[m[32m    err.hint =[m
[32m+[m[32m      'Vercel → Project → Settings → Environment Variables → enable each for Production → Save → Redeploy. If a value is Sensitive, it is hidden in the UI after save but still counts — re-paste if unsure.';[m
     throw err;[m
   }[m
 [m
[36m@@ -70,6 +102,15 @@[m [masync function spotifyGet(path, token) {[m
 }[m
 [m
 export default async function handler(req, res) {[m
[32m+[m[32m  applyCors(req, res);[m
[32m+[m
[32m+[m[32m  if (req.method === 'OPTIONS') {[m
[32m+[m[32m    res.setHeader('Access-Control-Allow-Methods', 'GET, OPTIONS');[m
[32m+[m[32m    res.setHeader('Access-Control-Allow-Headers', 'Content-Type');[m
[32m+[m[32m    res.setHeader('Access-Control-Max-Age', '86400');[m
[32m+[m[32m    return res.status(204).end();[m
[32m+[m[32m  }[m
[32m+[m
   if (req.method !== 'GET') {[m
     res.setHeader('Allow', 'GET');[m
     return res.status(405).json({ error: 'Method not allowed' });[m
[36m@@ -122,9 +163,12 @@[m [mexport default async function handler(req, res) {[m
     });[m
   } catch (e) {[m
     const status = e.status || 500;[m
[31m-    return res.status(status).json({[m
[32m+[m[32m    const body = {[m
       error: e.message || 'Server error',[m
       detail: e.detail,[m
[31m-    });[m
[32m+[m[32m    };[m
[32m+[m[32m    if (Array.isArray(e.missing)) body.missing = e.missing;[m
[32m+[m[32m    if (e.hint) body.hint = e.hint;[m
[32m+[m[32m    return res.status(status).json(body);[m
   }[m
 }[m
[1mdiff --git a/package-lock.json b/package-lock.json[m
[1mindex 1b8c3544..b1782b20 100644[m
[1m--- a/package-lock.json[m
[1m+++ b/package-lock.json[m
[36m@@ -14,6 +14,7 @@[m
         "@testing-library/user-event": "^13.5.0",[m
         "emailjs-com": "^3.2.0",[m
         "framer-motion": "^12.38.0",[m
[32m+[m[32m        "js-sha256": "^0.11.1",[m
         "react": "^18.2.0",[m
         "react-dom": "^18.2.0",[m
         "react-router-dom": "^6.3.0",[m
[36m@@ -13717,6 +13718,12 @@[m
         "jiti": "bin/jiti.js"[m
       }[m
     },[m
[32m+[m[32m    "node_modules/js-sha256": {[m
[32m+[m[32m      "version": "0.11.1",[m
[32m+[m[32m     